apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "baknine-helm.fullname" . }}-test-connection"
  labels:
    {{- include "baknine-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "baknine-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
